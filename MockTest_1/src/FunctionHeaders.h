long long countCommonStars(long long start1, long long end1,
	long long start2, long long end2,
	long long p);

int canSavePrincess(long* dragonPower, long* energyPortion, int noOfDungeons);

struct CityNode * meetingPoint(struct CityNode * deepikaStartCity,struct CityNode *mayaStartCity,
	int *numberOfCitiesMayaShouldTravel);

void fillSnakeBoard(int** board, int number, int x, int y, int direction);

int** initialSnakePosition(int len);

long long findTriplets(int *A, int len);